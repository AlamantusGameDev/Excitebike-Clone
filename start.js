OS.S.defaultStep = 1 / 30;
OS.S.numberOfScriptsToLoad = 1;

// Oversimplified.DEBUG.showMessages = false;
OS.SetCamera({
	width: 640,
	height: 480,
	hBorder: 320,  // Keep character completely centered at all times.
	vBorder: 240
});

function start () {

}
